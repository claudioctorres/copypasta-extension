# Research Pass 2 — Verification (2026-09-15)

Independent check of the claims the plan depends on. Verdicts: CONFIRMED / REFUTED / PARTIALLY / COULD-NOT-VERIFY. Every CONFIRMED/REFUTED has an in-session fetched URL + verbatim quote.

| # | Claim | Verdict | Evidence | Design impact |
|---|---|---|---|---|
| 1 | An `await` before `permissions.request` in the click handler loses the user gesture | **CONFIRMED** (mechanism); time window unknown | `permissions_api.cc`: `if (!user_gesture() && !ignore_user_gesture_for_tests && …) return RespondNow(Error(kUserGestureRequiredError));` — first statement of `PermissionsRequestFunction::Run`. `service_worker_data.cc`: `if (params.is_user_gesture) { scoped_extension_interaction = ExtensionInteractionProvider::Scope::ForWorker(...); } bindings_system()->DispatchEventInContext(...)` — gesture is a stack-scoped object alive only during synchronous dispatch. chromium-extensions thread: "In case you need to run any async function (e.g. against the Chrome API) this will not work anymore." | `permissions.request` is the first call in `onClicked`; everything else after it resolves |
| 2 | `request` on an already-granted origin resolves `true` without a prompt | **CONFIRMED** | `permissions_api.cc`: `// If all permissions are already active, nothing left to do. if (total_new_permissions->IsEmpty()) { constexpr bool granted = true; return RespondNow(WithArguments(granted)); }`. Doc: "After a permission has been removed, calling `permissions.request()` usually adds the permission back without prompting the user." Caveat: the gesture check still precedes it. | disable path may go through `request` |
| 3 | `registerContentScripts` rejects patterns outside granted hosts | **REFUTED** — registers silently; access enforced at injection | `scripting_api.cc` only has `kEmptyMatchesError = "Script with ID '*' must specify 'matches'."`; `content_script_utils.cc` `ParseMatchPatterns` validates syntax only; `user_script_set.cc`: `if (injector->CanExecuteOnFrame(...) == PermissionsData::PageAccess::kDenied) { return injection; }` | check `granted === true` before registering |
| 4 | Persistent registrations survive restart but are wiped on update/reload | **CONFIRMED** | `scripting_constants.h`: `kRegisteredScriptsStorageKey` = `"dynamic_scripts"` "persists across sessions". `state_store.cc`: `void StateStore::OnExtensionWillBeInstalled(..., bool is_update, ...) { RemoveKeysForExtension(extension->id()); }` — fires for updates too. Thread: "The reload button has the side effect of unregistering content scripts registered with registerContentScripts()." | reconcile on `onInstalled` + `onStartup` |
| 5 | Granted optional host permissions survive updates/restarts | **PARTIALLY** (source yes, docs indirect) | `extension_prefs.cc`: `// The set of permissions that were granted at runtime … constexpr const char kPrefRuntimeGrantedPermissions[] = "runtime_granted_permissions";` — only `OnExtensionUninstalled` deletes prefs. Doc: "When you upgrade your extension, Chrome won't disable it for your users if the upgrade adds optional rather than required permissions." | reconcile from `permissions.getAll().origins` needs no re-prompt |
| 6 | `tab.url` visible on click via activeTab; via host permission otherwise | **CONFIRMED** | activeTab doc: "Get the URL, title, and favicon for that tab via an API that returns a tabs.Tab object (essentially, "activeTab" grants host permission temporarily)." tabs doc: "This property is only present if the extension has the "tabs" permission or has host permissions for the page." | guard `tab.url === undefined` |
| 7 | activeTab + scripting injects into sub-frames of the clicked tab | **PARTIALLY** — top-frame origin only | `active_tab_permission_granter.cc`: `new_hosts.AddOrigin(valid_schemes, url);` (main-frame URL). `script_executor.cc`: `if (!scripting::HasPermissionToInjectIntoFrame(...)) { return … kContinue; }` | immediate injection = same-origin frames; documented |
| 8 | `insertCSS({origin:'USER'})` beats page `!important`; registered `css` is AUTHOR-only | **CONFIRMED** | scripting doc: `CSSInjection.origin` "Defaults to 'AUTHOR'"; `RegisteredContentScript` has no origin field. MDN cascade: author `!important` (5) < user `!important` (6). | content.js asks SW for `insertCSS({origin:'USER'})` |
| 9 | Manifest `name` ≤45, `short_name` ≤12, `description` ≤132; store title = manifest name | **PARTIALLY** — name limit is **75** | "The "name" key (required) is a short, plain text string (maximum of 75 characters)"; "maximum of 12 characters recommended"; "no more than 132 characters"; "After uploading your item, you won't be able to edit the metadata of your manifest in the developer dashboard." | limits in manifest test |
| 10 | Registration fee is US$5 | **COULD-NOT-VERIFY** | Official: "pay a one-time registration fee" / "in an amount determined in Google's sole discretion". $5 only in community pages. | say "one-time fee (US$5 per community reports)" |
| 11 | No injection on Web Store / chrome:// / other extensions | **CONFIRMED** | `chrome_extensions_client.cc`: `if (extension_urls::IsWebstoreDomain(url)) { *error = manifest_errors::kCannotScriptGallery; return false; }`; `extension_urls.cc`: `kNewChromeWebstoreBaseURL[] = "https://chromewebstore.google.com/"`, `kChromeWebstoreBaseURL[] = "https://chrome.google.com/webstore"`; `permissions_data.cc` `IsRestrictedUrl` for `chrome:` and other extensions' schemes. | `originOf` denylist + "can't run here" badge |
| 12 | `keypress` never fires for Ctrl/Cmd+letter | **PARTIALLY** | Blink `web_frame_widget_impl.cc`: "Suppress the next keypress event unless the focused node is a plugin node." — set only when the RawKeyDown was handled (e.g. editor Copy command). Not proven for every case. | also intercept `keypress` with the same filter |
| 13 | Playwright: extensions in headless via `channel:'chromium'`; SW via `serviceWorkers()`/`waitForEvent('serviceworker')` | **CONFIRMED** | "Note the use of the chromium channel that allows to run extensions in headless mode. Alternatively, you can launch the browser in headed mode." `@playwright/test` latest **1.63.0** | pin 1.63.0 |
| 14 | `node --test` default globs; Node LTS | **CONFIRMED** | "`**/*.test.{cjs,mjs,js}`, `**/*-test.{cjs,mjs,js}`, `**/*_test.{cjs,mjs,js}`, `**/test-*.{cjs,mjs,js}`, `**/test.{cjs,mjs,js}`, `**/test/**/*.{cjs,mjs,js}`"; "describe() is an alias for suite(), and it() is an alias for test()". LTS: **v24.21.0 "Krypton"** (2026-09-07). | target Node 24 |
| 15 | Twemoji 🍝 SVG exists, CC-BY 4.0; resvg-js version | **CONFIRMED** | `1f35d.svg` starts `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 36 36"><ellipse fill="#939598" cx="18" cy="24" rx="18" ry="10"/>`; `LICENSE-GRAPHICS` first line "Attribution 4.0 International"; `@resvg/resvg-js` **2.6.2** | pin 2.6.2; attribution line |
| 16 | `tabs.query({url})` works with host permission and no `tabs` | **CONFIRMED** | query.url: "This property is ignored if the extension does not have the "tabs" permission or host permissions for the page." | badge refresh across tabs |
| 17 | Stop flag is on the Event, shared across worlds | **CONFIRMED** | `event.h`: `void stopImmediatePropagation() { immediate_propagation_stopped_ = true; }`; `event_target.cc`: `if (event.ImmediatePropagationStopped()) { break; }`; `event_dispatcher.cc`: `if (event_->PropagationStopped()) return kDoneDispatching;` — no world logic. | core mechanism safe |
| 18 | Store images: 1280×800 or 640×400; promo tile required? | **PARTIALLY** — 640×400 not on the page | "At least one 1280x800 px screenshot, up to 5 total." / "A PNG or JPEG file that is 440x280 px to use as the small promo tile." / "you must provide the following promotional images and video, with the exception of the Marquee promo tile, which is optional" | 1280×800 screenshots, 440×280 tile, 128 icon |

Fetch failures: issues.chromium.org/40057101 (sign-in shell); uievents HTML (truncated; used GitHub source); several chromium raw paths 404 (found under other directories); worker global scope timeout not located.
